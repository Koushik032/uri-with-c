#include<stdio.h>
int main()
{
    int x,count=0;

    double sum=0,avg;

    while(1)
    {
       scanf("%d",&x);

       if(x<0)
       {
           break;
       }

       sum+=x;
       count++;

    }
        avg=sum/count;
        printf("%.2lf\n",avg);

    return 0;
}
